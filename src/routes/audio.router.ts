import { Router } from "express";
import validatorHandler from "../middlewares/validator.handler";
// import {
//     createUserSchema,
//     getUserSchema,
//     updateUserSchema,
// } from "../schemas/user.schema";
// import {
//     getUsers,
//     postUser,
//     getUsersById,
//     putUser,
// } from "../controllers/user.controller";

// const router = Router();

// router.get("/", getUsers);
// router.get("/:id", validatorHandler(getUserSchema, "params"), getUsersById);
// router.post("/", validatorHandler(createUserSchema, "body"), postUser);
// router.put(
//     "/:id",
//     validatorHandler(getUserSchema, "params"),
//     validatorHandler(updateUserSchema, "body"),
//     putUser
// );
// export default router;
